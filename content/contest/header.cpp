#include <bits/stdc++.h>

#ifndef ONLINE_JUDGE

#include "debug-header.cpp"

#else
#define debug(...) (void)(0)
#endif

using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    return 0;
}
